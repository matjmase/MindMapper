import { AuthenticatedDirective } from './authenticated.directive';

describe('AuthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthenticatedDirective();
    expect(directive).toBeTruthy();
  });
});
